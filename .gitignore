# Dependencies & builds
node_modules
.next
.next-tauri
out
build/
extensions/*/out
extensions/*/node_modules
extensions/*/*.vsix
public/monaco
.api-hidden
next-env.d.ts
tsconfig.tsbuildinfo

# OS
.DS_Store

# Tauri / Rust
src-tauri/target/
src-tauri/.tauri-signing.key

# Local build outputs (copy into landing/downloads manually)
dist-windows/
/vault/

# Secrets & local config (never commit real keys)
.env
.env.*
!.env.example
*.pem
*.key
credentials.json
secrets.json
**/local.config.json
